[package]
name = "git-repo-migrator-application"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
git-repo-migrator-domain = { path = "../domain" }
git-repo-migrator-git-runner = { path = "../git-runner" }
git-repo-migrator-platform-core = { path = "../platform-core" }
git-repo-migrator-workspace = { path = "../workspace" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
